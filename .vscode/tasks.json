{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build bookstore",
      "type": "shell",
      "command": "mvn clean install",
      "options": {
        "cwd": "${workspaceFolder}/bookstore"
      }
    }
  ]
}